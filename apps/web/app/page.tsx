"use client";
import { FormEvent, useEffect, useState } from "react";
import type { AtlasStatus, Decision, Mission } from "@atlas/types";
const api = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:3333";
export default function Home() {
  const [status,setStatus]=useState<AtlasStatus|null>(null),[missions,setMissions]=useState<Mission[]>([]),[decisions,setDecisions]=useState<Record<string,Decision>>({});
  const [title,setTitle]=useState("Analisar oportunidade de mercado"),[objective,setObjective]=useState("Avaliar se existe demanda suficiente para lançar um novo serviço B2B"),[context,setContext]=useState(""),[busy,setBusy]=useState<string|null>(null),[error,setError]=useState("");
  useEffect(()=>{void refresh()},[]);
  async function refresh(){try{const[s,m]=await Promise.all([fetch(`${api}/atlas/status`),fetch(`${api}/missions`)]);if(!s.ok||!m.ok)throw new Error();const list=await m.json() as Mission[];setStatus(await s.json());setMissions(list);setError("");const loaded=await Promise.all(list.filter(x=>x.decisionId).map(async x=>[x.id,await(await fetch(`${api}/decisions/${x.decisionId}`)).json()] as const));setDecisions(Object.fromEntries(loaded))}catch{setError("A API do Atlas não está acessível. Inicie API e Web e atualize a página.")}}
  async function createMission(e:FormEvent){e.preventDefault();setBusy("create");setError("");try{const r=await fetch(`${api}/missions`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({title,objective,context})});if(!r.ok)throw new Error("Não foi possível criar a missão");const mission=await r.json() as Mission;setMissions(c=>[mission,...c]);setTitle("");setObjective("");setContext("")}catch(e){setError(e instanceof Error?e.message:"Erro inesperado")}finally{setBusy(null)}}
  async function execute(m:Mission){setBusy(m.id);setError("");try{const r=await fetch(`${api}/missions/${m.id}/execute`,{method:"POST"});if(!r.ok)throw new Error("A análise falhou");const d=await r.json() as Decision;setDecisions(c=>({...c,[m.id]:d}));setMissions(c=>c.map(x=>x.id===m.id?{...x,status:"completed",decisionId:d.id}:x))}catch(e){setError(e instanceof Error?e.message:"Erro inesperado")}finally{setBusy(null)}}
  return <main className="mission-shell"><aside className="sidebar"><a className="brand"><span className="brand-mark">A</span>atlas<span className="muted">.os</span></a><nav><a className="nav-link active">Mission Control</a><a className="nav-link">Projetos</a><a className="nav-link">Conhecimento</a></nav><div className="core-state"><span className={`state-dot ${status?.lifecycle==="running"?"online":""}`}/><div><strong>{status?.lifecycle==="running"?"Atlas operacional":"Conectando"}</strong><small>{status?`${status.ai.provider} · ${status.ai.model}`:"Verificando núcleo"}</small></div></div></aside><section className="content mission-content"><header className="topbar"><div><span className="eyebrow">MISSION CONTROL</span><h1>Decisões com contexto,<br/><em>do começo ao fim.</em></h1></div><div className="status-pill"><span className="state-dot online"/>{status?.modules.filter(x=>x.status==="ready").length??0} módulos prontos</div></header>{error&&<p className="error-banner">{error}</p>}<section className="mission-grid"><form className="panel mission-form" onSubmit={createMission}><span className="eyebrow">NOVA MISSÃO</span><h2>O que o Atlas deve analisar?</h2><label>Título<input value={title} onChange={e=>setTitle(e.target.value)} maxLength={160} required/></label><label>Objetivo<textarea value={objective} onChange={e=>setObjective(e.target.value)} minLength={10} maxLength={2000} required/></label><label>Contexto <small>opcional</small><textarea value={context} onChange={e=>setContext(e.target.value)} maxLength={5000} placeholder="Mercado, restrições, sinais já observados..."/></label><button className="new-button" disabled={busy==="create"}>{busy==="create"?"Criando...":"Criar missão"}</button></form><section className="panel system-panel"><span className="eyebrow">ATLAS CORE</span><h2>Estado do sistema</h2><div className="system-row"><span>Lifecycle</span><strong>{status?.lifecycle??"—"}</strong></div><div className="system-row"><span>IA</span><strong>{status?.ai.mode==="mock"?"Modo local":"Provider externo"}</strong></div>{status?.modules.map(x=><div className="module-row" key={x.name}><span className="state-dot online"/>{x.name}<small>{x.status}</small></div>)}</section></section><section className="history"><div className="panel-title"><div><span className="eyebrow">HISTÓRICO RECENTE</span><h2>Missões e decisões</h2></div><button className="text-button" onClick={()=>void refresh()}>Atualizar</button></div>{missions.length===0&&<div className="panel empty-state">Nenhuma missão criada ainda.</div>}<div className="mission-list">{missions.map(m=>{const d=decisions[m.id];return <article className="panel mission-card" key={m.id}><div className="mission-heading"><div><span className={`mission-status ${m.status}`}>{m.status}</span><h3>{m.title}</h3><p>{m.objective}</p></div>{m.status!=="completed"&&<button className="new-button" disabled={busy===m.id} onClick={()=>void execute(m)}>{busy===m.id?"Analisando...":"Executar análise"}</button>}</div>{d&&<div className="decision"><span className="eyebrow">DECISÃO</span><h4>{d.recommendation}</h4><p>{d.rationale}</p><div className="confidence"><span>Confiança</span><strong>{Math.round(d.confidence*100)}%</strong><i><b style={{width:`${d.confidence*100}%`}}/></i></div><h5>Próximos passos</h5><ol>{d.nextSteps.map(step=><li key={step}>{step}</li>)}</ol><small>Provider: {d.provider} · {d.model}</small></div>}</article>})}</div></section></section></main>
}
