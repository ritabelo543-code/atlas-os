{
  "name": "atlas-os",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.16.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test && node scripts/check-infrastructure.mjs",
    "test:infra": "node scripts/check-infrastructure.mjs"
  },
  "devDependencies": {
    "@types/nodemailer": "^8.0.1",
    "turbo": "^2.5.5"
  },
  "dependencies": {
    "jimp": "^1.6.1",
    "nodemailer": "^9.0.5"
  }
}
